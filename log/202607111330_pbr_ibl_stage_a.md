# PBR / split-sum IBL — Stage A: ops, builders, real-time viewer

## Goal

learnopengl.com/PBR (Cook-Torrance + split-sum IBL) を Dressi の記法で実装する
第一段。最終成果物は同一 forward グラフを共有する 3 サンプル
(①リアルタイムビュワー ②マテリアル最適化 ③Envmap 最適化)。本ログは
①まで (Stage 0 + A)。

## What was done

### Dependencies / assets (Stage 0)
- tinyexr v1.0.9 を FetchContent header-only パターンで追加 (zlib は
  asset_utils.cpp の stb 実装を再利用: `TINYEXR_USE_STB_ZLIB=1`)。
  OpenEXR は実ビルド+Imath 依存で重いため不採用 (PolyHaven の scanline
  EXR には tinyexr で十分)。
- FetchAssets.cmake: 既存 glTF-Sample-Models クローンブロックを拡張して
  DamagedHelmet もコピー (新しい取得機構は追加しない)。
- `LoadGltfScene` (examples/common/asset_utils): NORMAL/TANGENT 読み込み
  (欠落時は面積重み法線 / Lengyel 接線生成)、ノード変換の CPU 適用
  (DamagedHelmet は回転クォータニオン持ち)、マテリアルテクスチャの
  tinygltf デコード取り込み、**V 反転なし** (glTF は top-left 原点。
  既存 LoadGltfMesh の 1-v は OBJ 由来でそのまま)。
- `LoadImageExr` / `SrgbToLinear` / `DownsampleHalf` 追加。

### New ops (src/core/f_ops.cpp, codegen, core/ibl_math.h)
- `F::EquirectSample(map, dir)` — 方向→equirect uv→手動 bilinear
  (u wrap / v clamp)。ゼロ方向 (ラスタ背景) は 0。forward-only。
- `F::TextureBilinear(tex, uv, inv_uv)` — 手動 bilinear (clamp)。
  **テクスチャ微分可能**: backward `__gather_inv_uv_bilinear__` は既存
  `__gather_inv_uv__` の変種で、アンカー画素の ±3 窓の全寄与画素を
  forward のテント重みで**積算**する (nearest 版は1画素のみ)。
- `F::IrradianceConv(env, out)` — 全ソーステクセルの決定論 cos·sinθ 直和、
  格納は E(N)/π (定数環境 L0 → 出力 L0 がテストの錨)。
- `F::PrefilterEnv(env, out, rough, n)` — GGX importance sampling
  (Hammersley は手動 bitreverse で CPU/GPU bit 同一)、V=N=R、NoL 重み。
- `F::BrdfIntegrationLut(size, n)` — ゼロ入力 (ScreenCoord 前例)、
  VEC2 (A,B)、Smith-G k=r²/2。
- `F::AvgPool2x2` — 既存 Reduce2x2Sum×0.25 の1行ラッパー (微分可能)。
- 全 CPU カーネルは `core/ibl_math.h` の共有実装を呼ぶ (GLSL 側は
  codegen が emit する `dressi_*` ヘルパーが双子)。サンプラーは一切
  触らない (全 nearest のまま): HW bilinear は CPU パリティを壊し、
  clamp サンプラーは equirect シームを壊すため手動 4 タップ一択。

### Builders (include/dressi/ibl.h, src/core/ibl.cpp)
論文 Appendix 形状: `BuildIrradianceSample` (AvgPool→64×32→conv、既定
32×16)、`BuildPrefEnvironmentSample` (レベル別 Variable×5、128×64→8×4、
mip 不在による論文からの逸脱)、`BuildBrdfIntegrationMap` (256², n=512)。
ランタイム側 `SampleIrradiance` / `SamplePrefEnvironment` (LOD テント重み
blend = 隣接2レベル線形補間) / `SampleBrdfLut`。

### Shared forward graph + viewer example
- `examples/common/pbr_graph.{h,cpp}`: G-buffer (Rasterize×4: uvh/normal/
  tangent/wpos) → TextureBilinear×5 → TBN 法線マッピング → 点光
  Cook-Torrance (test_cpu_eval.cpp:209 の式 + 距離減衰) → IBL ambient
  (kD·irr·albedo + pref·(F0·A+B))·AO → emissive → ScreenCoord+カメラ基底
  uniform による環境背景 → Reinhard+gamma。`SafeNormalize` (0除算→0) が
  背景画素の NaN 汚染を防ぐ要。inv_uv は引数 (ビュワーは未アップロードの
  ダミー — forward は inv_uv を消費しないため合法)。
- `examples/pbr_shading/`: 自動オービット + VkViewer、毎フレーム同一
  リーフ集合 {clip, cam×4, light×2} を `execStepWithSendsAndRecvImgsStacked`
  で 1 submit。ヘッドレス時は全周 PNG。≥20 フレーム除外の median 表示。

### Codegen fix
special op の SamePixel 入力 (dir/uv) は Naive packing で slt に分類される
が generic load 対象外だった → `needs_generic_load` に
`__equirect_sample__`/`__sample_bilinear__` の第2オペランドを追加。

## Measurements (RTX PRO 6000, 512², DamagedHelmet 14.5k verts / 15.5k faces, env 512×256)

- 初回ビルド ~239 ms (vulkan-build 128 + upload 83; 2048²×5 テクスチャ)。
- **reactive cache が IBL 前計算をプルーニング: 12 stages (warmup) → 5 (定常)**。
  前計算 (irr 32×16 + pref 5 levels + LUT 256²) はウォームアップ中のみ実行。
- 定常 execStep+readback median **1.35–1.49 ms/frame = 670–740 FPS**
  (gpu-execute ~1.2 ms; silhouette 0.26 ms 比の増分は RASTER×4 と
  2048² マテリアル×5 + IBL 7 サンプル/px の融合 FRAG パス)。
- テスト: 既存 92 → **120 全緑** (CPU: 解析値/シーム連続性/定数環境の錨、
  TextureBilinear 中心差分勾配; GPU: 両 packing パリティ tol 1e-3、
  reactive rebuild 後の結果不変、bilinear 回復最適化)。

## Key decisions

- bilinear gather の安定学習率は nearest 版と別物 (Σw² 倍): 回復テストの
  発散 (NaN) はこれが原因で lr=15/500iter に調整。CLAUDE.md に記録。
- V 反転問題: normal G-buffer のリブ模様が正常に出たことで UV 正当性を確認
  (公式スクリーンショットも暗い緑黒のヘルメットであり見た目は正しい)。

## Open issues / next

- Stage B: pbr_material_optimization (複数視点、albedo のみ最適化、他 GT)。
  inv_uv は texture_optimization 同様 UV 空間 Rasterize で構築。
- Stage C: Envmap 最適化 — EquirectSample backward (小マップ=WIDE gather、
  背景=要計測)、IrradianceConv 転置 gather、prefilter の決定論直和化。
- TextureBilinear の clamp 域 (テクスチャ端) では gather が HW clamp の
  重み畳み込みを無視する (nearest 版と同じ近似哲学)。

## Stage B addendum (same session): pbr_material_optimization

- **DamagedHelmet の TEXCOORD_0 は V∈[1,2]** (REPEAT 前提の整数オフセット)。
  clamp サンプラーでは最下行に張り付き、UvAsClip では全 UV 三角形が
  NDC 外にクリップされて inv_uv テーブルが空 → 勾配が完全ゼロという形で
  顕在化 (adam_t は進むが sum|m|=0、inv_uv valid 0/262k が決め手)。
  LoadGltfScene で軸ごとに floor(min) を引いて整数周期を除去
  (三角形が整数境界を跨ぐアセットは未対応、コメントに明記)。
  修正後はビュワーの見た目も本来の DamagedHelmet (ティールのバイザー、
  発光リング) になった — 修正前は底行クランプ + ジオメトリ由来の陰影で
  「それらしく」見えていた。
- examples/pbr_material_optimization: 複数視点 (6)、albedo (または --optimize=mr)
  のみ最適化・他は GT。ビューごとに G-buffer を1回ラスタライズし GT/pred の
  2回シェーディング (pbr_graph を BuildPbrGBuffer/BuildPbrShade に分割)。
  loss は gamma 前の Reinhard 画像 (Pow の 0 での勾配発散を回避)。
  in-graph Adam (addUpdate、silhouette の流儀)、Sobol サブピクセルジッタ。
- 結果 (512² アトラス、256²×6 views、lr=0.02): 2000 iter で loss 1.9e-2 →
  8e-5、被覆テクセル PSNR 21.2 dB、3.9 ms/iter (定常 ~4ms; ウォームアップ
  で IBL 前計算×1回 + inv_uv ラスタ×6 は静的なのでプルーニング)。
  回復アトラスは GT の構造 (白パネル/バイザー/デカール) を正しい位置に再現。

## Stage C addendum (same session): pbr_envmap_optimization

- **勾配パス**: `F::EquirectSample` / `F::IrradianceConv` に厳密転置の
  backward を追加 (決定論線形写像なので転置が厳密、atomics 不要)。
  - `__equirect_sample_bwd__ k=`: 各 map テクセルが全 dir 画素を走査し
    forward の 4 タップを再導出して gy を積算。map テクセル数だけの
    スレッドではレイテンシバウンド (64×32 map × 4×192² 画面で 45ms) →
    **WIDE 分割** ({map_w, map_h·K} の水平バンド partial + `__tile_sum__`)
    で ~1ms ({V,1} 頂点 gather と同じ罠と同じ処方)。K は
    map_texels·K ≥ 64k を目安に自動選択。
  - `__irradiance_conv_bwd__`: 各 env テクセルが irradiance マップ全出力
    (512) をループする転置 gather。1 パス、軽量。
  - AvgPool チェーンは既存 bwd で伝播。PrefilterEnv は forward のみ
    (毎 iter 現在の env から specular を再レンダするが env 勾配は流さない
    — importance sampling に安価な転置がないため。直和版 prefilter が
    将来パス、CLAUDE.md に記録)。
- examples/pbr_envmap_optimization: 複数視点 (仰角を交互 ±)、env 以外
  すべて GT。GT 枝は静的でプルーニング、最適化枝の IBL 前計算は毎 iter
  再実行 (env が dirty のため — 設計どおり)。in-graph Adam、loss は
  Reinhard 後 gamma 前。PSNR はトーンマップ後の値で評価 (生 HDR MSE は
  太陽テクセルに支配される)。
- 結果 (env 64×32、192²×6 views): 1500 iter で loss 0.101 → 0.0086、
  接触成分 PSNR 11.8 dB (8 views/lr0.08 で 13.1 dB)、**median 5.8 ms/iter**
  (GPU ~3.5ms; equirect 転置 2.6ms + prefilter forward 0.6ms + シェーディング)。
  回復 env は観測視円錐 (空の青 / 地面の緑 / 天頂の明部) を正しく再現し、
  未観測領域は暗いまま + ヘルメット遮蔽部は bg 勾配なし — 素直な
  逆レンダリングの挙動。レンダはターゲットとほぼ一致 (残差は勾配なしの
  specular 経由)。
- テスト: 128 全緑 (追加: EquirectSample/IrradianceConv の中心差分勾配、
  diffuse チェーン end-to-end、GPU 勾配パリティ (setGradOutputsEnabled +
  inputGrads 読み)、GPU env 回復ループ)。

## Stage C-2 addendum: 微分可能プレフィルタ (PrefilterConv)

ユーザ指摘「プレフィルタの畳み込みをバックワードすれば env 全体に色が付く
のでは」→ そのとおりだった。ヘルメットはほぼ金属で、光沢反射が背景レイの
届かない球面領域を広く拘束する。

- **`F::PrefilterConv(env, out, rough)`**: importance sampling の代わりに
  GGX NDF 重みの決定論畳み込み `pref(t) = Σ_s w·L / W(t)`
  (w = D_ggx(cos θ_h)·max(R·d,0)·sinθ, V=N=R, α=max(rough,0.045)²)。
  固定線形写像なので **転置が厳密** (`__prefilter_conv_bwd__`)。
- **正規化係数 W(t) はゼロ入力の静的オペ** (`__prefilter_conv_norm__`) —
  env の値に依存しないため、env が毎 iter dirty でもプルーニングされる。
- `BuildPrefEnvironmentSampleConv`: トップレベルを (プール後) ソース解像度
  以下にクランプ — 64×32 env に 128×64 プレフィルタは情報ゼロで
  スクリーン勾配 gather だけ4倍になる (これで 27→12.7ms/iter、実測定常
  ~8.7ms)。`BuildPbrIblMaps(env, differentiable_prefilter=true)` で切替、
  **GT/pred 両ブランチを同じ定式化にする**こと (混ぜると定式化差が回復
  env に押し付けられる)。
- 結果 (env 64×32、192²×6 views、1500 iter): loss 0.0086 → **0.0011**、
  env PSNR 11.8 → **17.7 dB**。回復マップは全域にもっともらしい色
  (青空+太陽・地面の緑・地平線の帯)。残ノイズは極付近 (sinθ→0 の
  弱拘束領域) のみ。テスト 130 全緑 (定数環境の厳密性、specular チェーン
  の中心差分勾配、GPU パリティに PrefilterConv 項追加)。
